# AGENTS.md

本仓库用于 iOS 壁纸项目，通过高频小步的方式研发。

## 最高优先级规则
1. 任何编码前，必须先澄清需求并得到用户确认。
2. 每项任务必须拆到足够细，便于独立开发、独立验证、独立提交。

## 强制工作流
在开始任何实现前，先输出：
- 目标
- 本轮范围
- 不做什么
- 验收标准
- 风险点
- 待确认项
- 最小任务切片列表

未得到用户明确确认前：
- 允许做文档、方案、拆解
- 不允许进入代码实现

涉及订阅、VIP、价格、权益、恢复购买、审核文案的任务时，在开始任何实现前还必须额外确认：
- 付费方式是否仅为 Apple 应用内订阅
- VIP 权益边界与非 VIP 限制
- 订阅周期、价格档位、是否包含试用
- 是否需要恢复购买入口与对应用户路径
- 是否需要本地校验、收据校验或服务端能力
- 是否涉及账号体系、云同步、跨设备权益同步

## 任务拆解要求
- 一个任务只做一件事
- 一个任务尽量只影响一个模块或一条链路
- 一个任务必须能单独提交
- 一个任务如果同时涉及多个层次，需要继续拆分

## 提交要求
- 一任务一提交
- 每次完成一轮可验证修改后，立即提交一个 commit，便于随时回滚
- 不混入无关改动
- 提交信息使用清晰前缀：`docs:`、`chore:`、`feat:`、`fix:`、`refactor:`、`test:`

## 产品约束
- 该项目是本地优先的 `iOS 壁纸应用`
- 默认不依赖云端
- 默认不引入账号体系与云同步，除非用户明确确认
- 默认不记录敏感明文日志
- 涉及权限、鉴权、加密、生物识别时必须先确认方案
- 首发支持 Face ID / Touch ID
- 首发支持从分享面板导入
- 首发不支持后台播放
- 最低支持 `iOS 16`
- 首发必须包含 Apple 应用内订阅能力
- 首发必须提供 `VIP / 会员` 付费能力
- 首发必须提供 `恢复购买` 入口
- `VIP` 权益、非 VIP 限制、试用策略未确认前，不进入实现
- 未确认前不接入 Apple 订阅之外的其他支付方式
- 订阅页文案、价格展示、自动续费说明、恢复购买说明必须满足 App Store 审核要求
- 首发目标为 App Store 上线
- 若采用轻量封装存储，必须明确其不是强密码学加密

## 实现原则
- 优先简单方案
- 优先系统原生能力
- 优先最小可用实现
- 不做未经确认的扩展需求


<claude-mem-context>
# Memory Context

# $CMEM HeartWall 2026-04-19 11:07am GMT+8

No previous sessions found.
</claude-mem-context>
